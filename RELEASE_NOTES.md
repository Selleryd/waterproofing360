# R5 / 5.0.0 — final logo and motion update

Based directly on the approved R4 ZIP.

## Requested changes

1. Live 3D opens with Rain and Orbit selected and running; Reset restores the same defaults.
2. The hero's Pause effects button is removed. Existing hero effects run by default.
3. The actual uploaded PNG replaces the previous generated logo in the header, mobile menu, and footer. The favicon uses the original shield pixels.

Reduced-motion system preferences, the footer motion setting, offscreen suspension, and manual Rain/Orbit switches remain available. Manual orbit controls intentionally stop automatic rotation, preserving inspection behavior.

## Scope retained

All original galleries and service associations; all eight researched articles; all service pages; the property guide and project brief; search; the detailed cutaway; the existing 3D geometry and renderers; responsive layout; all original real-project and architectural images.

The update also guards deferred model-resize callbacks when navigating away, so a disposed model is not accessed. Rain/Orbit button states now remain synchronized after keyboard Home/reset and manual keyboard rotation.

## Publication

Not published. Not pushed to GitHub. Review indexing remains disabled until the real production address is explicitly configured.
